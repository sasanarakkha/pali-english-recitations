---

name: Build documents
'on':
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    container: sasanarakkha/document-build-actions:latest
    permissions:
      contents: write
    steps:
      - uses: sasanarakkha/document-build-actions@v1
        with: {}
